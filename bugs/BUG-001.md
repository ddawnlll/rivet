# BUG-001 — Workspace test gate failed during a moving source snapshot

**Status:** OPEN / BLOCKED-UNSTABLE-TREE  
**Priority:** P0  
**Scope manifest:** read-only workspace diagnostics; no writes authorized while another cargo/TUI process and external edits are active.

## Symptom and evidence

[OBSERVATION] `cargo test --workspace` exited 101 at capture time with:

```text
error[E0560]: struct `CompletionProposal` has no field named `proposal_id`
error[E0433]: cannot find type `ProposalId` in this scope
```

Receipt: [.rivet/audit/baseline/04-cargo-test.txt](/C:/Users/dresden/Documents/rivet/.rivet/audit/baseline/04-cargo-test.txt), exit 101 in `04-cargo-test.exit.txt`.

## Hypothesis and discriminating observation

[INFERENCE] The compilation observed a non-atomic combination of an edited ACCP unit test and its changing type definition. The discriminating observation is to stop the external edit stream, record status, and rerun `cargo test --workspace`; if the same failure remains in one stable tree, this hypothesis is falsified and the ACCP test/type mismatch becomes a normal patch candidate.

## Acceptance criteria

- Stable `cargo test --workspace` completes with exit 0.
- No test is deleted, skipped, weakened, or changed in meaning.
- `cargo fmt --check`, strict clippy, and `cargo check --workspace` also pass in the same stable snapshot.

## Non-goals

Do not revert `.rivet-auth.json`, external source edits, Cargo dependency changes, or any test semantics. Do not patch until the user releases the active process/edit stream.

## Verification commands

```powershell
git status --short --branch
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo check --workspace
cargo test --workspace
```

No implementation diff, review, or verifier receipt exists yet.
