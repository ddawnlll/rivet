# Abstract

Bugünkü coding agent'ların baskın paradigması, frontier modelin kullanıcı isteğini yorumladığı, repository'yi tekrar tekrar context içine aldığı, araçlarla etkileştiği ve bir sonraki adımı yeniden inference ile seçtiği **context-centric model loop**'udur. Bu paradigma güçlüdür çünkü modern foundation modeller novel architecture, ambiguous intent, causal diagnosis, code synthesis ve unfamiliar repository interpretation gibi semantik olarak açık uçlu görevlerde bugün sahip olduğumuz en genel motorlardır. Rivet v0.2 bu gerçeği başlangıç varsayımı olarak kabul eder; modeli pasif bir coprocessor'a indirgemeyi hedeflemez.

Problem modelin “fazla güçlü” olması değil, **cognition'ın geçici, probabilistic ve context-dependent; software project'in ise persistent, revisioned ve authority-bound olmasıdır.** Bir model invocation'ı doğru hipotezi kurabilir, ancak sonraki session aynı rejected yaklaşımı yeniden keşfedebilir; doğru project constraint'i okuyabilir, fakat alakasız artifact retrieval'ı altında onu kaçırabilir; başarılı bir test sonucunu görebilir, fakat o testin hangi claim'i gerçekten doğruladığını aşırı genelleyebilir. Bu nedenle uzun-horizon agent reliability problemi yalnızca daha büyük context veya daha iyi prompt problemi değildir.

Rivet'in v0.2 araştırma tezi şudur: **aynı frontier LLM, persistent evidence-bound hard epistemic state, task/session-scoped bounded soft workspace ve task-conditioned Cognitive View Compiler ile eşleştirildiğinde context-centric eşdeğer bir agente kıyasla daha tutarlı ve/veya daha verimli uzun-horizon software engineering davranışı gösterebilir.** Noesis bu hard/soft epistemic state'i taşır; Praxis belirli predicates'i scoped ve mekanik biçimde doğrular; ACCP evidence admission, state promotion ve action authority sınırlarını uygular; Hephaestus ise normal cognition tekrarlayan biçimde tıkandığında cold-path reframing sağlar.

Generalistlik pre-defined project memory veya language-specific kernel branch'lerinden gelmez. Yeni bir repository önce ucuz bir deterministic census ile gözlemlenir; ardından LLM, hierarchical relevance frontier üzerinden hangi klasör ve artifacts'ın semantik olarak incelenmeye değer olduğuna karar verir. `node_modules`, `vendor`, `target` veya generated trees global olarak “junk” ilan edilmez; çoğu taskta `DEFER` edilir, task değiştiğinde yeniden aday olabilir. Böylece expensive indexing tüm repository'ye uygulanmadan LLM-led project ontology kademeli olarak kurulur.

Rivet'in ilk deneysel hedefi minimum model invocation değildir. İlk hedef **state quality**'dir: ne kalıcı olmalı, ne provisional kalmalı, model hangi representation üzerinden reasoning yapmalı, yanlış state nasıl invalidate edilmeli ve persistent memory unutkanlığı çözerken epistemic ossification yaratıyor mu? Model-call, token ve wall-clock azalması secondary outcome olarak ölçülür; başarı veya reliability pahasına invocation azaltmak başarı sayılmaz.

Bu belge v0.1'deki literature audit, Bun production case study, verification, concurrency, security, capability ve benchmark bölümlerini korur; ancak araştırma önceliğini değiştirir. v0.2'nin minimum falsification prototype'ı dört çekirdek bileşeni izole eder: **Noesis Hard State, Noesis Soft Workspace, Cognitive View Compiler ve Hierarchical Repository Relevance.** Bu katmanlar aynı model ve aynı temel execution capabilities altında ölçülebilir bir avantaj üretmezse daha karmaşık scheduler, semantic capability protocol, multi-agent veya distributed architecture yatırımı ertelenir veya reddedilir.
